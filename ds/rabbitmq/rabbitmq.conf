# Кластер собирается статическим списком узлов: адреса известны заранее из
# compose, внешний discovery не нужен.
cluster_formation.peer_discovery_backend = classic_config
cluster_formation.classic_config.nodes.1 = rabbit@rabbit-1
cluster_formation.classic_config.nodes.2 = rabbit@rabbit-2
cluster_formation.classic_config.nodes.3 = rabbit@rabbit-3

# При расколе кластера меньшая часть останавливается: лучше отказ, чем две
# копии очереди, разошедшиеся по содержимому.
cluster_partition_handling = pause_minority

loopback_users.guest = false
log.console.level = info
